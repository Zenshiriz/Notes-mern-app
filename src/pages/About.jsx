import React ,{useContext, useEffect} from 'react'
import NoteContest from '../context/notes/noteContext'

export default function About() {
  const a = useContext(NoteContest)

  return (
    <div>
      about 
    </div>
  )
}
